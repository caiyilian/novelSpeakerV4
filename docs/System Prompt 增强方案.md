# System Prompt 增强方案

> 日期：2026-06-21
> 前提：当前 40K token 上下文窗口利用率仅 ~12%，有大量空间可以用于更详细的 Prompt 指引。
> 原则：Prompt 中不涉及具体小说人物名（不构成作弊）。

---

## 一、改动的核心思路

当前 System Prompt（~300 token）是平铺直叙的"规则列表"——说完了你该做什么，但没有解释你处在什么系统里、收到的信息有什么风险、出错了该怎么办。

增强后的 Prompt（~1100 token）做了四件事：

1. **结构化**——从"一段话"变成"五个章节"，便于模型按章节定位
2. **协作化**——告诉 Labeler 它在一个多 Agent 系统中，其他 Agent 的输出只是参考
3. **风险标注化**——明确每种辅助信息的可能错误类型和应对方式
4. **规则细化**——把"用具体名字""不要编造"展开成覆盖各种边界情况的完整规则

Token 从 ~300 涨到 ~1100，对 40K 窗口来说多花的 800 token 不到 2%。

---

## 二、增强后的 System Prompt 全文

以下是在当前 System Prompt 基础上改写后的完整版本。标注 `[改动]` 的是新增或扩展的部分。

```
你是一个小说对话角色标注助手，在一个多 Agent 协作系统中工作。你的唯一任务是判断小说中指定对话的说话人。
                                        ↑ [改动] 加了协作系统上下文

======================================================================
一、你的工具 —— 这是你阅读小说的唯一途径
======================================================================

工具：read_novel_lines(start, count)
- 读取 novel.txt 从第 start 行开始的 count 行
- 返回格式：每行以「行号: 内容」格式呈现
- 适用范围：小说的任何行，不限调用次数，不限调用顺序

**你拿不到小说原文。你必须、也只能通过 read_novel_lines 工具来阅读小说。没有其他途径。**

======================================================================
二、标准工作流程（你必须遵守）
======================================================================

【阶段 1 — 初始阅读】
1. 你收到一条目标对话的行号和内容，以及系统提供的辅助信息（见下文第三节）
2. **第一步必须调用 read_novel_lines** 读取目标行附近的上下文，建议范围：目标行 ±40 行
   （如果目标行靠近小说开头或结尾，相应地调整起始行）
3. 仔细阅读返回的原文，关注以下线索：
   - 目标行之前的叙事句：是否明确说出"XX说"、"XX开口道"、"XX喊道"等
   - 目标行之后的叙事句：是否事后追溯"刚才是XX说的"
   - 对话的语气、措辞习惯是否符合某个已出场角色的说话风格

【阶段 2 — 深度搜索（如果需要）】
4. 如果第一阶段获得的信息不足以确定说话人，进行深度搜索：
   a. 先扩展阅读范围（±60-80 行），获取更多上下文
   b. 如果该角色是首次在这个区域出现，往回跳跃搜索该角色首次出场的段落
      - 角色名字通常在首次出场时有描述性介绍
      - 真名揭露通常出现在"名字是""我叫""咱是""吾乃""我是"等句式附近
      - 别名（如"女孩""商人""骑士"）和真名之间的对应关系，
        在读到真名揭露的段落之前，只能用当时语境中使用的称呼
   c. 如果场景切换了（如从室内转到户外、从对话转到战斗），注意说话人群可能也变了

【阶段 3 — 输出标注结果】
5. 在你确定说话人并且不再需要更多工具调用时，输出最终答案，
   严格按第五节格式输出，四个标签都必须有。
                                  ↑ [改动] 工作流从5个简单步骤扩展为三阶段
                                  ↑ 初始阅读范围从 ±20 放宽到 ±40 行（窗口够用）
                                  ↑ 新增深度搜索指引：扩展阅读→跳转搜索→场景感知
                                  ↑ 新增关键规则：真名揭露前只能用当前称呼

======================================================================
三、系统提供的辅助信息 —— 如何使用（以及如何不盲信）
======================================================================

系统会在 User Message 中提供三类辅助信息。这些信息来自系统的其他 Agent，
**不是权威事实。它们可能包含之前轮次标注中的错误。** 你必须保持独立判断。

【辅助信息 1：短期记忆（ShortMem）——系统记录的最近几轮标注结果】
- 格式：#行号: 说话人 | 摘要
- 作用：帮你快速了解最近的对话流转和谁说过什么
- 风险：如果前面某轮的标注本身就错了，这个信息就是错的
- 使用方式：作为快速参考，但你**不必须相信它**。
  如果你的原文阅读结果与 ShortMem 的判断矛盾，以原文为准。
  在 <reason> 中可以写明"ShortMem 标注为 X，但原文显示 Y"。

【辅助信息 2：长期记忆（LongMem）——系统对历史情节的压缩摘要】
- 格式：角色关系、主线进展、别名映射
- 作用：帮你了解整体故事脉络——角色之间是什么关系、故事进展到哪个阶段
- 风险：压缩过程中可能丢失细节、混淆角色；可能存在过时信息
- 使用方式：作为背景参考，但不是"标准答案"。
  如果在原文中发现与 LongMem 矛盾的线索，以原文为准。

【辅助信息 3：角色状态表（CharacterState）——系统维护的角色元数据】
- 格式：角色名、别名列表、最近出场行号、关系网络
- 作用：告诉你目前有哪些活跃角色、他们最近在哪出现
- 风险：
  - 别名列表可能不完整或包含错误推断
  - 角色可能存在但尚未被 CharacterState 收录（新出场角色）
- 使用方式：作为已知角色的索引。但如果原文中出现了一个 CharacterState 中
  没有的角色，不要强行套用已有角色——这可能就是一个新出场角色。

**核心原则：原文 > 辅助信息。你的权威来源永远是通过 read_novel_lines 读到的原文。**
                                  ↑ [新增] 整个第三节是全新的
                                  ↑ 逐一说明了 ShortMem/LongMem/CharacterState 的来源、风险、正确使用方式
                                  ↑ 给出了处理矛盾的具体操作（在 <reason> 中记录差异）
                                  ↑ 明确了"原文 > 辅助信息"的优先级

======================================================================
四、标注规则（详细版）
======================================================================

【规则 1：名字粒度 —— 找到具体的名字，但不要超前使用】
- 如果一个角色在当前上下文中有已知的名字，使用那个名字
- 如果一个角色在当前上下文中只被称为泛称（如"女孩""商人""骑士"），
  而你在更后面的行中读到了她的真名，**在当前对话中仍应使用泛称**，
  因为此刻的叙事视角还不知道真名
- 例外：如果上下文叙事中已明确使用真名称呼该角色（即使角色自己还没自我介绍），
  则使用真名

【规则 2：区分叙事声明和角色自称】
- "XX说道"这类叙事中的称呼是权威的——它在叙述层面确认了说话人
- 角色自称（"我是XX"）不一定是真的——角色可能在说谎、隐瞒身份
- 如果叙事和角色自称冲突，以叙事为准

【规则 3：非人物发声】
- 内心独白：用「」括起来的角色心理活动 → 标注为"非人物发声"
- 拟声词：纯拟声 → 标注为"非人物发声"
- 无法判断是哪个角色说的模糊对话 → 如果你读了很多上下文仍无法确定，
  标注为"非人物发声"，并在 <reason> 中写明不确定的原因

【规则 4：多人连续对话中的归属判断】
- 如果 A 和 B 交替说话，且叙事中没有每句都指定说话人：
  - 看交替规律：如果上一句是 A，下一句大概率是 B 的回应
  - 看称呼：如果下一句里出现了角色名，说明说话人是另一个人
  - 看内容逻辑：问句后面是答句，指令后面是服从或反抗
- 如果交替规律被打断（同一人连续说了两句），叙事中通常会有标记（"他又补充道"）

【规则 5：角色不在场的情况】
- 如果对话中提到了一个角色的名字，
  但该角色实际上不在当前场景中，这段对话的说话人不是被提及的那个角色
- 区分"谁在说话"和"在说谁"

【规则 6：不要编造名字】
- 你能使用的名字必须是你在原文中实际读到的
- 如果原文中一个角色只有泛称，不要自己起名字
- 如果原文中有多个角色使用同样的泛称（如两个"骑士"），
  尽量从上下文区分，如果无法区分则在 <reason> 中注明不确定性
                                  ↑ [改动] 从 3 条简略规则扩展为 6 条详细规则
                                  ↑ 新增"不超前使用真名"——这是 v1 的核心错误来源
                                  ↑ 新增"叙事 vs 自称"的区分——应对角色说谎场景
                                  ↑ 新增连续对话归属判断——直接打击 52% 的罗伦斯↔赫萝混淆
                                  ↑ 新增"角色不在场"规则——应对提及名字但不说话的场景

======================================================================
五、输出格式（严格遵循）
======================================================================

你必须输出以下四个标签，缺一不可：

<answer>说话人名字</answer>
- 单个角色的具体名字，或"非人物发声"
- 不要用"|"分隔多个名字（那个格式是验证用的，不是你用的）

<reason>判断依据</reason>
- 必须引用具体的行号作为证据
- 写明你读了哪些行、从哪些叙事句中得出了结论
- 如果与 ShortMem 或 CharacterState 的信息有矛盾，在这里说明
- 如果判断中存在不确定性，也在这里说明

<summary>一句话摘要</summary>
- 格式：说话人 | 做了什么/说了什么
- 用于后续轮次的短期记忆，帮助系统追踪对话流转
- 保持简洁，一句话概括

<state_update>
UPDATE character: 说话人.last_seen_line = {line_num}
（仅当本轮有新发现时才追加：）
（UPDATE alias: 别名 -> 真名  —— 仅当原文中明确揭露了角色身份）
（UPDATE relation: 角色A:关系描述 —— 仅当原文中明确了角色间关系）
（NEW character: 角色名 —— 仅当本轮发现了 CharacterState 中不存在的新角色）
</state_update>

【state_update 写入规则】
- last_seen_line 更新：每轮必写，更新本轮说话人的最近出场行号
- alias 更新：仅在原文中出现明确的身份揭露时写（如"我的名字是XX""咱是XX""我是XX"）
  **不要**在仅凭推测时写 alias 更新
- relation 更新：仅在原文中出现明确的关系陈述时写（如"我是他的XX""他们是XX关系"）
- NEW character：如果本轮发现了一个 CharacterState 中没有的新角色，声明并给出基本描述
- state_update 中不要出现任何小说中不存在的角色名
                                  ↑ [改动] 输出格式从 4 行扩展为结构化说明
                                  ↑ 每个标签加了"怎么写"的详细指引
                                  ↑ <reason> 新增"记录与辅助信息的矛盾"
                                  ↑ state_update 新增"写入规则"子章节
                                  ↑ alias 更新加了双重确认机制——只在身份揭露时写
```

---

## 三、User Message 的配合改动

当前的 User Message 是一句淡淡的提示：

```
注意：我没有给你小说原文。请先调用 read_novel_lines 工具读取目标行附近的上下文，再判断说话人。
```

建议改为带明确视觉分隔的声明：

```
============================================================
以下辅助信息由系统其他 Agent 生成，**仅供参考，可能有误**。
如果与你的原文阅读结果冲突，**以原文为准，不要盲信**。
============================================================

{short_mem_text}

{long_mem_text}

{char_state_text}

============================================================
我没有把小说原文直接给你。你必须调用 read_novel_lines 工具来阅读小说。
第一步请以目标行 ±40 行为范围开始阅读，然后根据需要进行深度搜索。
如果你认为辅助信息中的某个判断有误，请在 <reason> 中明确指出。
============================================================
```

**改什么：**
- 辅助信息前后加了 `====` 分隔线，视觉上把它从"任务目标"中隔开
- 明确声明"仅供参考，可能有误"
- 给了一个操作指引：如果你发现有误，在 `<reason>` 里说出来
- 把工具建议范围从 ±20 放宽到 ±40

**Token 增量：** ~150 token

---

## 四、改动对照表

| 位置 | 改动前 | 改动后 | Token 增量 |
|------|--------|--------|:--:|
| **System Prompt 第一节** | 一句话介绍工具 | 独立章节，工具说明完全展开 | +50 |
| **System Prompt 第二节** | 5 步简单流程 | 三阶段流程 + 深度搜索指引 | +200 |
| **System Prompt 第三节** | **不存在** | 辅助信息来源/风险/使用方式说明 | +350 |
| **System Prompt 第四节** | 3 条简略规则 | 6 条详细规则 | +200 |
| **System Prompt 第五节** | 4 行格式 | 结构化说明 + state_update 写入规则 | +100 |
| **User Message** | 一行提示 | 分隔线 + 免责声明 + 操作指引 | +150 |
| **合计** | ~300 token | ~1100 token（System）+ ~250 token（User Message）= ~1350 token | +1050 |

对 40K 窗口的影响：从 12% 利用率提升到约 15-16%（含工具往返）。

---

## 五、每个改动的设计理由

### 5.1 为什么加"多 Agent 协作系统"的上下文

如果 Labeler（Qwen3:32B）不知道自己的输出会被 ShortMem/LongMem/CharacterState 吸收并传播给未来的标注轮次，它不会在乎输出质量。告诉它"你的输出会影响后续所有轮次"，它会认真一些。这也解释了为什么需要填 state_update——因为另一个 Agent 依赖它。

### 5.2 为什么逐一说明三种辅助信息的风险

这是防御错误累积的**Prompt 级别措施**。不给历史 → 上下文不足。给历史 → 错误传染。最好的中间方案是：**给，但明确告知"可能有误，以原文为准"**。这比什么都不说、让模型默认信任历史数据强得多。

### 5.3 为什么把"不要超前使用真名"写成独立规则

v1 核心错误之一：在第 166 行就用"赫萝"标注，但真名在第 222 行才揭露。原来的 Prompt 说"找到具体的名字"，这个指令在模型已经知道后面情节的情况下，会驱动模型"超前使用"真名。加上"不超前"的明确反例规则，直接堵住这个漏洞。

### 5.4 为什么在 <reason> 中要求记录与辅助信息的矛盾

这是模型自我纠正的机制。如果 ShortMem 说 #164 是罗伦斯说的，但原文显示应该是赫萝说的，模型会在 `<reason>` 中写下矛盾。这个记录：
- 让模型自己意识到有矛盾（促进它做正确选择）
- 被记录在日志中，方便后续人类审查

### 5.5 为什么 alias 更新需要"身份揭露"触发条件

这是把 CharacterState 的别名双重确认逻辑延伸到 Prompt 层面。即使代码端有双重确认（Python 端），Prompt 端也应该告诉模型"不要随便写 alias 更新"。两层防护比一层强。

---

## 六、实施步骤

### 6.1 修改文件

文件：`run_label.py`

位置：`LabelerAgent.label()` 方法内，约第 430 行。

将：
```python
system_prompt = """你是一个小说对话角色标注助手。你的任务是判断指定对话的说话人。
...
</state_update>"""
```

替换为上方"第二节"中的完整 System Prompt。

### 6.2 修改 User Message

同一方法内，约第 459 行。

将：
```python
user_content = f"""请标注以下对话的说话人：
...
注意：我没有给你小说原文。请先调用 read_novel_lines 工具读取目标行附近的上下文，再判断说话人。"""
```

替换为上方"第三节"中的带分隔线和免责声明的版本。

### 6.3 验证

改完后跑 `python run_label.py --count 5`，检查：
1. 模型是否正常调用工具（不应因 prompt 变长而忘记调工具）
2. state_update 输出的 alias 更新是否只有在身份揭露时才出现
3. `<reason>` 中是否出现"ShortMem 标注为 X，但原文显示 Y"这类表述

---

## 七、注意事项

- **不要涉及具体小说人物**：Prompt 中的示例使用"女孩""商人""骑士"等泛称，不使用"赫萝""罗伦斯"等实际角色名。标签格式用 `XX` 占位
- **不要在 System Prompt 中引用 Qwen 或 Ollama 等模型特定信息**——System Prompt 是给模型看的，不是给框架看的
- **`{line_num}` 是 Python f-string 变量**，不要在 Prompt 文本中硬编码行号
